import Realm from 'realm';

export const userSchema = {
    name: "user",
    properties: {
    userId:"int",
     id: "int",
      title: "string",
      body: "string",
     
    },
    // primaryKey: "id",
  };

  